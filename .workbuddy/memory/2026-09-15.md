# 2026-09-15

## 全项目只读审查（未修改任何业务代码）

对 junqi_engine 做了一次方案层 + 代码层的全面审查。核心结论（供后续修复优先级参考）：

### 致命级（会导致训练完全无效）
1. `junqi/train_rl.py:1162/1174/1179` 热启动分支 `net = JunqiNet.load_from_file(...)` 重新绑定 `net`，
   但 `optimizer` 在 `:1114` 已绑定旧 net → 优化器与新网络脱钩，首轮起权重零更新。
   只有断点续训路径（`:1143-1152` 走 `load_checkpoint` 就地载入）正确。
2. `junqi/train_rl.py:1217` `torch.load(opp_path)` 取到的是 `net.save()` 的包装字典
   （键 `model_state/in_channels/...`），worker `:537` 却用 `load_state_dict(..., strict=False)`
   → 键全不匹配、静默通过 → **自博弈 "best 对手" 是随机初始化网络**（OPP_MIX 约 25% 对局）。
3. `junqi/expert/` 11 个模块引用不存在的 `state.Move` / `get_piece_at` / `current_turn` /
   `config.PIECE_RANKS` 等 → 整包 ImportError，属僵尸代码，主流程用的是 `junqi/search.py`。

### 方案/路线层
- `train_rl.py:596-760` 自建了一套门控（wilson/decide_promotion/evaluate_gate），
  与 `junqi/eval_gate.py:run_gate`（配对同牌 + 先后手 + Wilson + 三元 SPRT，质量更高）并行存在；
  训练主循环只用前者，且两方向用 `seed+i` 与 `seed+100000+i` **非配对同牌**，`device` 硬编码 cpu。
- 方案 `AI_TRAINING_AND_HUMAN_PLAY_PLAN.md:244` 要求认输局 ±1 计入 Value，
  代码 `train_rl.py:980-982` 相反（只留 Policy）。代码正确（防自证回路），方案未回写。
- CLI/函数默认数据集仍指向 `datasets/p1_v1`、`p1_v2`，而 anchor 用 `p1_v3` → 两套口径并存。

### 运营/性能
- `models/candidate_latest_buffer.pkl` 单文件 **3.8 GB**（×3 目录≈11 GB），每轮无条件全量 pickle 落盘。
- `venv/` 4.7 GB 位于工程目录内。
- MCTS 每次模拟 batch=1 前向；`predict_state` 内部 `legal_actions()` 计算两次；
  `position_key()` 整盘排序在根候选循环里逐动作构造。
- `search.py:769-771` IDS 早停判据为"总耗时 > 25% 预算"，语义错误（应为"预估下一层超限"），
  超时时返回 `(acts[0], -inf)`，会污染 `train_search_distill` 的蒸馏教师分布。
- `search.py:706` 根节点用收窄 alpha 窗口搜后续动作，fail-low 的上界分被当作精确分写入
  `stats.root_scores`；`ai.py:329-336` 回退路径把 1e5 量级 move-ordering 分当估值分返回 GUI。

### 确认无问题（避免重复排查）
- 估值函数未被 1e5 启发分污染：`bomb_suicide_exchange` / `camp_outstrike_bias` 仅在
  `search.py:124/132` 的 `_score_action()` 排序路径，终局分 WIN_SCORE=1e6 分界清晰。
- P1 数据集导出按**对局**切分（非 ply）、编码 `world=None`（公共视角），符合硬约束。
- 无循环依赖；核心 `state.py`(fan-in 43) / `config.py`(37) / `rules.py`(28) 结构健康。
- 官方终局码映射本体正确：`dataset.py:183-189`（1/21/22/23→±1，40/42/43→0，20/24→无 Value）。

---

## 修复落地（同日，P0 为主）

报告落在 `reviews/CODE_REVIEW_2026-09-15.md`。按优先级修复，全部带回归测试。

- **R1（致命）**：`net.py` 新增 `unwrap_state_dict` / `adapt_state_dict` / `load_state_dict_into`；
  `train_rl.py` 新增 `warmstart_candidate(net, optimizer, path, ...)`，主干超参一致时**就地**载入，
  不一致时重建 net 并**同时**重建 optimizer。三处热启动点改用该函数。
- **R2（致命）**：`train_rl.py` 新增 `load_weights_into_net` / `infer_net_architecture` /
  `build_opponent_net`。Worker 由裸 `load_state_dict(strict=False)` 改为规范化载入，
  缺键即返回 None（回落 greedy/expert），绝不返回随机假对手。
  顺带修掉只看 `in_conv.0.weight.shape[1]` 推断架构的隐患（非默认 128/6 检查点同样会静默退化）。
- **R3**：`eval_gate.run_gate` 新增 `init_states`（与 seeds 等长，两局共用同一初始局面）与
  `device` 透传；`train_rl.evaluate_gate` 逐场景委托 `run_gate`，删除失效的
  `_gate_game_job` / `_run_jobs` / `_tally`；device 不再写死 cpu。
- **P1**：`should_save_buffer()` 节流 + `save_checkpoint(save_buffer=...)` + `.tmp`/`os.replace` 原子写；
  CLI `--buffer-save-every`（默认 5，末轮恒写；0=除末轮外不写；1=旧行为）。
- **R4**：新增 `junqi/expert/DEPRECATED.md`（说明为何不可导入、复活路径）+ 守卫测试
  `tests/test_p0_expert_deprecated.py`（主流程零依赖）。

### 验收
- 基线 298 passed / 3 skipped → **326 passed / 3 skipped**（新增 28 项：
  test_p0_hotstart_and_opponent 10、test_p0_gate_pairing 6、test_p3_buffer_persistence 9、
  test_p0_expert_deprecated 3）。耗时约 107s。
- 未改动 RuleConfig 规则定义、终局奖励语义、信息边界；未触碰 models/best.pt。

### 踩坑备忘
- 本项目 bash 环境缺 coreutils（ls/grep/head/tail/wc/dirname 全无）。所有脚本任务改用
  `C:/Users/hu/.workbuddy/binaries/python/versions/3.13.12/python.exe` 或项目 venv
  `venv/Scripts/python.exe`（3.11.9 + torch 2.5.1 + pytest 9.1.1）直接执行，别用管道接 head/tail。
- `junqi/net.py::save()` 写出的是**包装字典** `{"model_state":...}`，与 `state_dict()` 不同；
  任何 `torch.load` 后都要先 `JunqiNet.unwrap_state_dict`。这是 R2 的根因，改别处时别再踩。
- `StratifiedReplayBuffer` 的样本契约：policy `(state, mask, target, phase)`、value `(state, z_cls, is_world)`，
  索引访问写死（`sample[3]` / `sample[1]`），写测试时不能用短元组。
- 未处理（按序留给下一轮）：C1/C2、C3、C7、C9、P2。（**已于同日全部完成，见下**）

---

## 第二批修复（同日完成，审查全部条目收口）

测试基线：298 → **403 passed / 3 skipped**。

### 搜索正确性
- **C1** `search.py` 新增 `should_stop_ids` / `ids_growth_estimate`：早停判据从
  "总耗时 > 预算 25%"（语义完全错误、浪费大部分预算）改为"已用 + 本层耗时×实测增长因子 > 预算"。
- **C2** 超时降级返回 `(兜底动作, 0.0)` + `stats.degraded=True`，**永不返回 ±inf**。
- **C3** `search(exact_root_scores=True)` 根节点全窗口搜索；新增 `stats.root_scores_bounded`；
  `ai.ExpertAgent.choose_actions` 在 `topn>1` 时自动启用（GUI 提示/蒸馏需要精确分）。
- **C4** topn 回退只返回 `[(best_act, 0.0)]`，不再把 1e5 排序分当估值分。
- **C5** `hybrid_engine.select_action` 返回 `None` 而不是 `Action("pass")`（后者缺 `frm` 必 TypeError）。

### MCTS / 热路径
- **C6** 新增 `mcts.tree_repetition_limit(state)`，跟随 `cfg.repetition_draw_count`（下限 2）。
- **P2** `net.predict_state(..., acts=, mask=)`；MCTS 用 `inspect.signature` 做能力探测后传入
  `acts`（避免破坏旧签名测试替身——曾一次性打破 9 个既有测试）。
- **P3/P5** MCTS 根 `avoid` 判定改为算一遍复用；`depth<=0` 的 QSearch 提到 Zobrist 之前；
  `_is_tactical` 闭包提升到方法级。

### 数据 / 方案 / 指标
- **C7** 新增 `dataset.outcome_bucket_from_meta`，统计与标签共用同一张终局码表
  （原统计另写一遍并把 code 24 计入 decided_win）。
- **R6** `DEFAULT_P1_DIR="datasets/p1_v3"` + `MIN_P1_VERSION=(3,0,0)`；5 个模块的默认路径统一；
  `load_p1_arrays` 加 `check_p1_version` 守卫，旧数据集直接拒载。
- **R5** 回写 `AI_TRAINING_AND_HUMAN_PLAY_PLAN.md` §6 Value 节（认输局只进 Policy），
  并补齐修订原因/影响范围/验证方法/回滚方案四项。
- **C8** `play_game` 估值失败改为计数 + 告警 + `rec["eval_failures"]`；顺带去掉重复调用
  `evaluate_expert`（4 次→2 次）；`StratifiedReplayBuffer.load` 失败不再静默。
- **C9** 新增 `elo_update_from_score`（标准 Elo 公式 + 按样本量夹紧），替换随机游走。
- **C12** train_bc / eval_bc / fit_weights / load_p1_arrays 补测试。

### 性能 / 附带发现
- **P4** 新增 `ai.load_net_cached`（path+mtime+device 缓存，容量 8，仅供推理）；
  `NNAgent` 与 `HybridDecisionEngine` 改走缓存（门控 256 局/轮原先 ~512 次反序列化 34MB 权重）。
- **P6** `apk_engine` 置换表改为**存完整 Zobrist + 命中校验**（原只存 18 位截断值，
  跨局面碰撞静默返回错误分数/PV），并加容量上界与插入序淘汰、浅条目不覆盖深条目。
- **C10 部分** 删除 `train_rl` 中从未被引用的 `PolicyDataset` / `ValueDataset`（及 `DataLoader`
  导入）——训练实际直采 `StratifiedReplayBuffer`。超长函数拆分未做（收益 < 回归风险）。
- **⚠️ 附带高危发现** `scripts/cleanup_models.py` 的删除模式含 `*_distilled.pt`，会把
  `models/value_distilled_v2.pt`（P3 热启动链首选的健康 Value 头）删掉，导致训练静默退回
  未校准的 BC 价值头。已改为：显式 `PROTECTED_EXACT` 保护名单 + **默认 dry-run**（`--yes` 才删）
  + 报告子目录占用。

### 未处置（需人工决策）
- `junqi/expert/` 选择"标记废弃 + 守卫测试"而非删除（文件已在 git 内，若要删是 `git rm -r junqi/expert/`）。
- `models/` 约 8.0 GB、`venv/` 约 4.7 GB 未自动清理（越界操作）。
- C10 的超长函数拆分（21 个 >120 行）。
