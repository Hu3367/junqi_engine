# junqi_engine 项目执行规则

## 总原则

本项目必须遵守 [AI_TRAINING_AND_HUMAN_PLAY_PLAN.md](AI_TRAINING_AND_HUMAN_PLAY_PLAN.md)。该文件是军棋翻棋规则判断、传统搜索增强、复盘数据、行为克隆、搜索蒸馏、自博弈训练和评估晋级的执行基线。

任何大模型或编码代理开始修改代码前，必须先阅读该文件，并在工作说明中标明当前修改所属阶段：`P0`、`P1`、`P2`、`P3` 或 `P4`。

## 必须遵守的硬约束

- `P0` 正确性验收未通过前，不得启动长时间自训练。
- 严格按官方 `list.cfg` 真实终局码赋予复盘 Value（认输 code 21、常规终局 code 1 均属明确胜负；协议和棋 code 40、限步判和 code 43 属正规和棋；不得把未终局或早期逃跑强退 code 20 误当成和棋 Value）。
- 公共 Policy 不得读取真实暗子身份；采样世界只能用于 Value 或完全信息验证。
- 不得添加吃子、挖雷等中间奖励改变终局目标。
- 不得将三个阶段实现成三个相互独立的模型。
- 不得只因训练 loss 下降就覆盖 `best.pt` 或宣布模型变强。
- 每项实现必须有对应测试或固定评测证据。
- 每次提交代码前检查 README 和 `docs/CHANGELOG.md` 是否需要同步更新。

若代码现状与方案冲突，必须先报告冲突和影响，不得静默绕过执行方案。
