# 军棋翻棋：人机对战与 AI 训练全局执行方案

> 状态：项目执行基线
> 
> 更新日期：2026-08-30
> 
> 适用范围：`junqi_engine/` 下的规则、搜索、复盘、监督学习、强化学习、评估和 GUI 集成

## 1. 先给结论

当前不应继续直接增加自训练轮数。项目的主要瓶颈不是网络容量，而是训练闭环的正确性、复盘数据没有接入深度训练，以及评估门控在大量和棋的环境下无法区分模型是否变强。

达到“可以和真人正常对战”的目标，不必先完成纯强化学习。最快、风险最低的路线是：

1. 修复规则搜索和 MCTS 的正确性问题；
2. 用复盘数据做行为克隆和搜索蒸馏；
3. 上线“神经网络提出候选、传统搜索验证”的混合引擎；
4. 将自博弈强化学习作为提高上限的后续阶段。

纯自训练适合长期追求超过传统搜索和稳定击败强人类棋手，不是达到第一版人机可玩水平的必要条件。

## 2. 规则与棋理一致性判断

### 2.1 已经一致的部分

| 你的判断 | 当前实现 | 结论 |
|---|---|---|
| 双方轮流翻棋或走棋 | `GameState.legal_actions()` 始终加入暗子翻棋，移动只从明子生成 | 一致 |
| 翻棋没有次数限制 | 暗子每回合都可以产生 `flip` 动作 | 一致 |
| 走棋只能走己方明子 | 只有 `revealed=True` 且颜色属于当前行动方的棋子生成移动 | 一致 |
| 隐藏信息推理量级较小 | `remaining_types()` 可根据已知构成维护暗子池，`sample_world()` 做有限世界采样 | 基本一致 |
| 开局翻棋、中期占营绞杀、尾盘清场/拖和 | `detect_phase()` 已按暗子数量和行营占用划分三阶段 | 一致，实证前20手进营率82.7%，吃子50.1%源自行营扑杀，证实“首翻子力即据点，依托行营辐射拓荒”与行营单向打击特权 |
| 劣势方利用雷和死区拖和 | `quiet/70`、重复局面和棋、`fortress_score()` 已有对应接口 | 棋理一致，静态模型仍不完整 |
| 优势方优先处理暗子两回合时差 | 规则层确实区分翻棋和移动，搜索可表达该节奏 | 一致，实证开局占营小子利用敌炸刚翻开不能移动时差主动贴身拆弹 |

### 2.2 需要保留的边界

“剩余暗子构成可以计算”不等于“可以知道每个暗子的位置身份”。网络在实战决策时只能使用公共信息；采样世界只能用于搜索中的价值估计，不能让策略头直接读取真实暗子身份。

因此采用以下信息边界：

- **公共状态**：明子身份、暗子位置、已阵亡构成、行营和铁路、计数器、阶段特征；
- **采样世界**：只在 Value 评估或完全信息战术验证中使用；
- **Policy**：训练和实战都以公共状态为输入；
- **Value**：可以对采样世界估值，再在根节点跨世界聚合。

三阶段应被视为课程采样和分析标签，不应训练三个互不相容的独立模型。阶段会随局面变化，模型必须学习从争营切换到筑垒、从筑垒切换到破阵的连续判断。

## 3. 当前工程审计结论

### 3.1 必须先修复的正确性问题

1. **MCTS 终局价值视角不一致**

   `GameState.apply()` 在设置胜者后会切换 `turn`。`mcts.py` 的终局价值按切换后的行动方计算，但反向传播又把价值当成胜者视角，导致一步吃旗的动作可能被传播成负价值。必须先增加强制吃旗测试，再修正价值视角约定。

2. **跨采样世界复用树节点会污染合法动作**

   不同暗子世界共享一棵树，但某一世界展开出的明子移动在另一世界中可能不存在。当前选择阶段没有按当前世界过滤节点动作，而 `GameState.apply()` 本身也不负责合法性校验。这会造成非法模拟或静默错误统计。

   第一版可采用每个确定化世界独立搜索并聚合根节点；若保留共享树，节点必须按当前可观察历史维护合法动作，并在选择时与当前世界的合法动作求交集。

3. **训练模型在门控失败时被完全回滚**

   `train_rl.py` 的门控失败路径会恢复 `best.pt`。现有 `elo_history.jsonl` 三轮均为 `0 胜 / 0 负 / 40 和`，因此这些轮次没有累积到下一轮。候选模型和生产模型必须分离：候选模型持续训练，`best.pt` 只负责晋级和发布。

4. **门控样本量和对手选择不适合和棋密集的规则**

   当前传统 `search2 vs search2` 已有 92.5% 和棋，`nn_mcts_30 vs search2` 的现有报告为 0 胜、2 负、8 和。40 局同模型对抗无法证明改进，也不能作为唯一晋级依据。

5. **随机数没有完全纳入实验种子**

   MCTS 的 Dirichlet 噪声使用全局 NumPy 随机源，训练主循环也有未绑定实验种子的 `random.random()`。必须由实验种子派生所有 Python、NumPy、PyTorch 和 Worker 随机源。

### 3.2 当前训练数据问题与官方数据库突破（2026-09-06 实证）

复盘目录约有 1000 局、约 106,274 个 ply。早期由于仅按着法流末尾是否“拔旗/吃光”判定，曾误得出“862 局未终局、仅 63 局分出胜负”的假象，导致训练管线人为抛弃了绝大多数终局 Value 标签。

2026-09-06 逆向破译官方数据库 `军旗复盘/list.cfg`（DES-ECB 加密）后，确认全部 1000 局均有官方权威终局判定，真实分布为：

- **常规拔旗/吃光终局（code 1）**：69 局（6.9%）；
- **主动认输/退房（code 21）**：456 局（45.6%），均在防守方子力崩塌或杀棋瞬间认输，具备高置信度胜负 Value 标签；
- **官方协议和棋（code 40）**：265 局（26.5%），双方协商认和，正规和棋 Value=0；
- **中途强退/逃跑（code 20）**：150 局（15.0%）；
- **限步判和与循环和棋（code 42, 43）**：36 局（3.6%）；
- **超时与断线判负（code 23, 24）**：23 局（2.3%）。

**重大突破与影响**：
可用 Value 真实终局样本从预估的 68 局暴增至近 800 局（核心玩家 458 胜、241 负、301 和）。这彻底解除了此前 Value 头因样本极度贫瘠（仅 63 胜局 + 5 和局）而迅速塌缩为“全部预测 Draw”的数据瓶颈。

现有 `fit_weights.py` 可以作为传统估值基线，但在正式使用前必须检查并修复隐藏身份泄漏：其特征函数遍历棋盘时不能把未翻开的军衔当作玩家可见信息，首翻定色前也不能使用未确定的座次颜色。

## 4. 是否必须自训练，以及更快的实现路径

### 4.1 目标分层

| 目标 | 是否必须纯自训练 | 推荐路径 |
|---|---:|---|
| 能和普通真人完成对局，少犯明显规则和战术错误 | 否 | 规则修复 + 传统搜索增强 + 复盘行为克隆 |
| 达到现有 `greedy/search2` 以上水平 | 否 | 搜索教师蒸馏 + 混合引擎 + 固定评测 |
| 稳定击败熟练真人，处理长程死区和复杂尾盘 | 部分需要 | 复盘预训练后进行自博弈和历史模型联赛 |
| 超过传统搜索的长期上限 | 基本需要 | 正确的 ISMCTS/自博弈、残局课程、规模化评估 |

### 4.2 最快可行路线

**第一条线：传统搜索增强。**

在现有 `Agent` 上补齐三类显式判断：行营势力范围、死区可达性、暗子翻开后的两回合时差，并加入重复和棋的根节点规避。使用复盘数据拟合权重，再用固定发牌和尾盘局面验证。这条线最快产生可玩的版本。

**第二条线：复盘行为克隆。**

对所有合法复盘动作训练公共 Policy。对明确胜负的状态训练结果 Value；对中止和未终局状态只训练 Policy。人类动作作为有噪声示范，不应被视为完美答案。

**第三条线：搜索蒸馏。**

在复盘状态和固定评测状态上运行 `search2/search3`，保存前若干候选动作的软分布和搜索分数。用软标签训练网络先验，使网络先学会传统搜索已经掌握的局部战术，再让 MCTS 负责扩展长程能力。

**第四条线：混合上线。**

实战中由网络负责候选排序和翻棋位置先验，传统搜索负责战术验证、强制吃旗、避免送死和和棋线管理。网络不可靠时自动回退到传统搜索。这比等待纯 RL 收敛更适合作为第一版人机对战引擎。

## 5. 分阶段执行计划

### P0：正确性和可复现性

**任务**

- 修复 MCTS 终局价值符号；
- 修复跨世界节点合法动作污染；
- 统一公共 Policy / 世界 Value 的信息边界；
- 统一 Python、NumPy、PyTorch、Worker 的随机数；
- 增加强制吃旗、强制困毙、强制拖和、重复局面测试；
- 保存完整 checkpoint，而不是只保存模型参数。

**验收**

- 固定种子下重复搜索，动作序列完全一致；
- 至少 1000 个固定种子搜索无非法模拟动作；
- 一步可以吃旗时 MCTS 必须把吃旗动作排在最高价值区域；
- 全量单元测试和新增测试全部通过。

在 P0 未通过前，禁止进行长时间训练和模型实力结论。

### P1：复盘数据集和传统基线

**任务**

- 解析 `.sav` 并重演合法状态；
- 按对局切分训练、验证、测试集，禁止按 ply 随机切分；
- 导出公共 Policy 样本：状态、合法动作掩码、人类动作、阶段标签；
- 导出 Value 样本：基于 `list.cfg` 官方解密真值，接入常规终局 (code 1)、主动认输 (code 21) 与协议和棋 (code 40) 的高质量三分类标签；
- 修复并运行 `fit_weights.py`，记录旧权重和新权重的镜像对局结果；
- 建立 random、greedy、search2、search3 四个固定基线。

**验收**

- 数据集可重复生成并带版本号和哈希；
- 所有样本动作在重演状态中合法；
- 早期无因逃跑与断线样本不进入结果 Value；
- 报告 Policy top-1/top-3、交叉熵和分阶段覆盖率。

### P2：行为克隆和搜索蒸馏

**任务**

- 先只训练公共 Policy，确认网络学会基本翻棋节奏和合法走子；
- 再加入搜索教师的软动作分布；
- Value 使用真实终局或完全信息搜索标签，保持世界模式输入；
- 训练一个混合代理：网络先验 + 传统搜索战术过滤。

**验收**

- 复盘测试集上的 Policy 指标显著优于随机；
- 固定开局、中盘、尾盘集合均无明显崩溃；
- 混合代理在相同计算预算下不弱于 `search2`；
- GUI 中出现明显非法、送旗、无意义循环的比例为零或接近零。

### P3：强化学习自博弈（闭环已运行，尚未通过 P4.4 长期挂机准入）

**任务**

- candidate 与 best 分离，门控失败不再清空训练进度（✅ 已实现）；
- 对手池包括当前模型、历史快照、greedy、search2 和少量随机策略（🔄 Worker 分支已实现，但实际 `best.pt` 占比和逐轮对手日志尚未验收）；
- 三阶段使用课程采样，但只保留一个共享网络（✅ 已实现）；
- 先从数百到数千局做短周期实验，再扩大规模（🔄 已完成原型轮次，结果未通过扩大训练准入）；
- 保持终局奖励为胜 `+1`、和 `0`、负 `-1`，禁止加入吃子、挖雷等中间奖励（✅ 严格遵守）；
- 标量 Value 升级为胜/和/负三分类头，并在编码器中接入 38 通道重复历史平面（✅ NN/MCTS 历史重复链路已接通并通过专项测试）。
- **数据质量改造（2026-09-13 修订，已实现）**：
  - **生成/评测夹具分离**：自对弈生成侧使用 `GENERATION_CFG`（no_capture_draw_plies=120、repetition_draw_count=4），调稀平局触发器以压缩 z=0 样本占比；**评测门控与靶场仍严格使用官方 RuleConfig() 默认规则**（70/1000/循环 3）。改变原因：expert 对局实测约 75% 为无吃子判和，弱模型自博弈和棋占比更高，z=0 样本无学习信号。影响范围：仅 train_rl 自对弈生成路径。验证方法：每轮自对弈打印决胜率与终局原因分布，门控原因拆分不得出现生成侧规则痕迹。回滚：GENERATION_CFG 换回 RuleConfig()。
  - **和棋尾部样本丢弃**：和棋局中 quiet ≥ 60 的样本段（生成夹具下必达判和、z 恒 0）不入回放池；决胜局样本全保留。回滚：quiet_tail_cutoff 设极大值。
  - **认输加速**：自博弈中走子方根 Value ≤ −0.95 连续 8 次己方回合（且 ply ≥ 40）判该方认输，对局按官方 **code 21（主动认输）语义**记明确胜负 ±1 终局标签。改变原因：人类权威数据 45.6% 以认输终局，认输判定用 Value 头自身输出捷径一个已定的结局，**不是中间奖励**——终局奖励定义（胜 +1 / 和 0 / 负 −1）不变，认输只是提前到达本已注定（连续 8 手深负评估）的终局，并使自博弈数据的胜负分布对齐人类真值分布。影响范围：train_rl 自对弈生成路径（含对手策略回合除外）。验证方法：ResignTracker 单测 + 认输集成测试（stub 网络确定性触发）+ 每轮自对弈决胜率统计 + gate 对照。回滚：resign_enabled=False（或 threshold=−2.0 永不触发）。**样本路由限制（2026-09-13 二次修订，实证后追加）**：认输局的样本只进 Policy 回放池、其 ±1 标签不进 Value 训练——首轮 500 局实证认输开启时 Value 三分类准确率 60%→16%（自证回路），关闭后 1 轮即恢复 60%；认输局的行棋对 Policy 仍有效，仅其自身判定的终局标签不得作为 Value 真值。

**准入复审结果（2026-09-01）**

- Epoch 3-5 合计 **1 胜 / 95 和 / 0 负**，其中 `repetition=85`；三个候选均 `promoted=false`，不能证明稳定压制；
- 当前 50 题 Value MAE 为 **0.4223**，三分类准确率为 **50.0%**；候选对 50 题全部预测 Draw，已发生类别塌缩；
- 初始复审时 95/95 单元测试通过，但缺少 NN/MCTS 历史重复链路的集成测试；
- 初始证据、根因和整改状态见 [P4.4 长期挂机准入复审与开启条件](docs/P4_EXECUTION_PLAN.md#p44-长期挂机准入复审与开启条件2026-09-01)。

**整改后状态（Epoch 6）**

- 99/99 单元测试通过，32 局门控 `repetition=0`，历史重复问题的专项修复有效；
- 候选门控为 1 胜 / 30 和 / 1 负，`promoted=false`，对 `search2` 参考得分为 `0.25`；
- Value MAE 为 `0.5690`、三分类准确率为 `20.0%`，预测分布为 0 Win / 35 Draw / 15 Loss，仍未达到健康训练信号要求；
- P4.4 长期训练采用“双开关”：启动训练必须先通过正确性、Value 健康度、实际对手分布、断点恢复、自动熔断和 3-5 轮中等规模稳定性；覆盖 `best.pt` 还必须另行通过每场景至少 200 局及 Wilson/Value 晋升门控。详细量化阈值见 [P4.4 长期挂机准入复审与开启条件](docs/P4_EXECUTION_PLAN.md#p44-长期挂机准入复审与开启条件2026-09-01)。

### P4：实战对弈、在线多世界推演与系统集成（原型可继续，P4.4 长期挂机未准入）

> **详细执行计划与数学模型设计详见独立执行方案**：[docs/P4_EXECUTION_PLAN.md](docs/P4_EXECUTION_PLAN.md)。

只有 P0-P3 的训练信号稳定后才进行：

**任务**

1. **贝叶斯暗子信念跟踪器（Bayesian Belief Tracker）**：
   - 维护全盘每个未翻开暗子位置的后验概率分布 $P(\text{PieceType} \mid \text{Observed History})$；
   - 结合对手行动（如“明知前方有大子却主动进击”或“被吃后回缩”）进行对手建模与暗子身份先验修正。
2. **在线多世界采样搜索（Online Determinization & PIMC/ISMCTS 混合）**：
   - 决策时从贝叶斯信念分布中采样 $K$ 个确定化棋局（Determinized Worlds）；
   - 在各世界内利用神经网络进行前向评估与快速战术验证，根节点汇总跨世界期望。
3. **GUI 胜率预测与交互增强**：
   - 在人机对战界面右侧实时显示当前局面的 Win / Draw / Loss 概率预测条；
   - 增加基于客观胜率的智能求和与认输判定。
4. **大规模持续自博弈演进（可选挂机）**：
   - 支持多卡/长期后台自对弈与定期 Champion/Challenger 自动晋升。
   - **当前禁令**：重复循环专项已修复，但 Value 质量、实际对手占比、自动熔断和中等规模稳定性仍未通过；在 P4 执行计划的启动硬门槛全部满足前，不得启动长期挂机。

## 6. 训练数据的正式标签规则

### Policy

- 所有合法复盘动作都可以用于 Policy；
- 训练输入必须是公共视角；
- 人类动作是示范，不是绝对最优动作；
- 可按阶段和局面难度加权，但不能删除尾盘拖和样本；
- 搜索教师使用软分布，不只保存单一 top-1 动作。

### Value

严格基于官方 `list.cfg` 解密判定赋予三级 Value 标签：
- **明确胜负局（+1 / -1）**：按行动方视角写入，包含常规终局（code 1）、主动认输（code 21）、超时判负（code 23）、长捉判负（code 22）；
- **正规和棋局（0）**：写入 0，包含官方协议和棋（code 40）、循环和棋（code 42）、限步判和（code 43）；
- **废弃/无价值标签（仅保留合法 Policy 动作）**：对局前期（如前 20 手）无实质交火即发生的偶发强退/逃跑（code 20）或断线（code 24），不可作为反映棋力高下的终局 Value；
- **自博弈认输局（2026-09-15 修订，与代码对齐）**：训练侧认输（ResignTracker 触发）判定的**终局事件**与官方 code 21 同语义，但**认输局的 ±1 标签不得进入 Value 训练池**——该局样本只进 Policy 回放池。
  - **修订原因**：本文件 §5 P3「认输加速」一节（2026-09-13 二次修订）与 `junqi/train_rl.py` 已先行改为「认输局仅留 Policy」，而本节原先写「认输局按 ±1 计入 Value」，两处自相矛盾。代码侧证据：2026-09-13 首轮 500 局实证，认输开启时 Value 三分类准确率 60% → **16%**，关闭后 1 轮即恢复 60%。根因是**自证回路**：认输判定取自 Value 头自身连续 8 手 ≤ −0.95 的输出，若再把该判定当作 Value 真值回灌，等于用未校准的 Value 去校准 Value。
  - **影响范围**：仅 `train_rl` 自博弈生成路径的 Value 样本池；Policy 样本、门控评测、官方 code 21 复盘 Value 标签（§6 第一条）均不受影响。
  - **验证方法**：`tests/test_resign_fixture.py` 认输样本路由断言 + 每轮 `Value 健康探针(p1_v3/test)` 平衡准确率不出现单轮断崖（阈值见 §5 P3 的 Value 健康度门槛）。
  - **回滚方案**：若要恢复「认输局 ±1 入 Value」，须同时提供打破自证回路的机制（例如改用独立的完全信息搜索标签、或对认输样本做重要性降权），并在 `docs/CHANGELOG.md` 记录对照实验的平衡准确率曲线；禁止直接删掉 `train_rl.py` 的样本路由过滤后重训。
- **自博弈认输的终局奖励语义**：终局奖励仍是胜 `+1` / 和 `0` / 负 `-1`，认输只是提前到达本已注定（连续 8 手深负评估、ply ≥ 40）的终局，**不是中间奖励**，因此不违反 §8 的禁止事项；
- 完整底牌可用于生成实际世界样本，但不能把真实暗子身份作为公共 Policy 输入。

## 7. 评估协议和晋级标准

评估必须同时覆盖：

1. 规则/战术单测：一步吃旗、雷、炸弹、困毙、循环和棋；
2. 固定 `opening`、`midgame`、`endgame` 局面；
3. 随机完整发牌；
4. 复盘测试集上的行为和搜索后悔值；
5. 相同计算预算下的 `greedy/search2/search3` 对抗。

每个阶段至少 200 局，固定种子，双方先后手各半。报告必须拆分：

- 胜、和、负以及 `胜=1/和=0.5/负=0` 得分；
- 只看有胜负对局的胜率；
- `no_capture`、`repetition`、`immobilized`、`flag` 的终局原因；
- Policy top-1/top-3、Value 校准、MCTS 访问熵；
- 非法动作数和同种子复现结果。

模型晋级不能只依赖 40 局同模型对抗或训练 loss。应使用固定基线、足够样本量和 Wilson 区间或 SPRT：整体不能显著退化，任一阶段不能严重退化，至少一个阶段需要有统计意义的改善。

## 8. 明确禁止事项

- 禁止在 P0 未通过前启动长时间自训练；
- 禁止把未终局或早期逃跑强退当作和棋 Value；严格以 `list.cfg` 官方终局码界定（主动认输 code 21 属明确胜负，协议和棋 code 40 属正规和棋）；
- 禁止将真实暗子身份输入公共 Policy；
- 禁止使用吃子、挖雷等中间奖励改变终局目标；
- 禁止为开局、中盘、尾盘训练三个互不衔接的独立模型；
- 禁止只看 loss 下降就覆盖 `best.pt`；
- 禁止用小样本、全是和棋的对局直接宣布模型超过传统搜索；
- 禁止为了提速牺牲动作合法性和实验可复现性。

## 9. 其他大模型的强制执行契约

任何其他大模型、编码代理或自动化脚本在修改本项目之前，必须：

1. 先读取本文件和项目级 `AGENTS.md`；
2. 说明本次修改属于 P0、P1、P2、P3 还是 P4；
3. 检查是否违反第 8 节禁止事项；
4. 先添加或更新对应验收测试，再修改实现；
5. 不得自行改变规则、奖励定义、信息边界或阶段模型架构；
6. 不得在 P0 未完成时建议或执行长时间训练；
7. 完成后必须报告：改动文件、实验种子、测试结果、评测结果、未解决风险；
8. 不能用“loss 下降”“程序能运行”替代实力评估；
9. 发现本方案与代码冲突时，先记录冲突并暂停相关阶段，不得默默绕过方案；
10. 每次提交代码前检查 README 和 `docs/CHANGELOG.md` 是否需要同步更新，并遵守项目提交规范。

本文件是训练和人机对战路线的执行基线。若需要改变路线，必须先修改本文件并说明改变原因、影响范围、验证方法和回滚方案。
